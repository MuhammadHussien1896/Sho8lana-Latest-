@using Microsoft.AspNetCore.Identity
@model ICollection<ServiceMessage>
@inject UserManager<Customer> UserManager
@{
    ViewData["Title"] = "DisplayMessages";
    var receiver = "" ;
}
<link href="~/assets/css/displayMessages.css" rel="stylesheet">

@*<h1>DisplayMessages</h1>
@if(Model == null){
    <h3>No Received Messages !</h3>
}else{
    @foreach(var item in Model)
    {
        if (UserManager.GetUserId(User) == item.CustomerId)
        {
            <h4>You : @item.MessageContent</h4>
            <p class="d-none">@(receiver = item.ReceiverId)</p>
        }
        else
        {
            <h4>@item.Customer.FirstName @item.Customer.LastName : @item.MessageContent</h4>
            <p class="d-none">@(receiver = item.CustomerId)</p>
        }
        <small>@item.MessageDate</small>
        <br />
        <a class="btn btn-primary" asp-controller="service" asp-action="chat" asp-route-id="@item.ServiceId" asp-route-receiverId="@receiver" >Send Message</a>
    }
}*@

<div class="container mt-5">
    <div class="row d-flex justify-content-center">
        <div class="col-md-8">
            <div class="headings d-flex justify-content-between align-items-center mb-3">
                <h5>Latest Messages :</h5>
            </div>
                @if(Model == null){
                    <h5>No Received Messages !</h5>
                }else{
                    @foreach(var item in Model)
                    {
                      <div class="card p-3 mb-5">
                        @if (UserManager.GetUserId(User) == item.CustomerId)
                        {
                            @*<h4>You : @item.MessageContent</h4>*@
                            <p class="d-none">@(receiver = item.ReceiverId)</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="user d-flex flex-row align-items-center"> <img src="https://i.imgur.com/hczKIze.jpg" width="30" class="user-img rounded-circle mr-2"> <span><small class="font-weight-bold text-primary">You</small> <small class="font-weight-bold">@item.MessageContent</small></span> </div> <small>@item.MessageDate</small>
                            </div>
                        }
                        else
                        {
                           @* <h4>@item.Customer.FirstName @item.Customer.LastName : @item.MessageContent</h4>*@
                            <p class="d-none">@(receiver = item.CustomerId)</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="user d-flex flex-row align-items-center"> <img src="https://i.imgur.com/hczKIze.jpg" width="30" class="user-img rounded-circle mr-2"> <span><small class="font-weight-bold text-primary">@item.Customer.FirstName @item.Customer.LastName</small> <small class="font-weight-bold">@item.MessageContent</small></span> </div> <small>@item.MessageDate</small>
                            </div>
                        }
                        @*<small>@item.MessageDate</small>*@
                        <br />
                        @*<a class="btn btn-primary" asp-controller="service" asp-action="chat" asp-route-id="@item.ServiceId" asp-route-receiverId="@receiver" >Send Message</a>*@
                    
                        
                            <div class="action d-flex justify-content-between mt-2 align-items-center">
                                <div class="reply px-4"> <a asp-controller="service" asp-action="chat" asp-route-id="@item.ServiceId" asp-route-receiverId="@receiver">Send Message</a> </div>
                                @*<div class="icons align-items-center"> <i class="fa fa-star text-warning"></i> <i class="fa fa-check-circle-o check-icon"></i> </div>*@
                            </div>
                      </div>
                    }
                }  
            
        </div>
    </div>
</div>

